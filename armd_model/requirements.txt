# Pinned to the exact versions used to train AND serve the artifacts.
# scikit-learn is load-bearing: newer versions fail to unpickle the joblib
# artifacts (`SimpleImputer has no attribute _fill_dtype`) and 500 the recommend
# endpoint. Train and serve with the SAME versions. (env: AURA_NEW, Python 3.11.15)
scikit-learn==1.3.2
pandas==2.1.4
numpy==1.24.3
joblib==1.5.3

# Evaluation-only (not required to train/serve the shipped RF artifacts):
#   shap        — per-prediction TreeSHAP explanations (M3/T3.1, armd_model has none directly; backend uses it)
#   lightgbm    — model comparison (M2, compare_models.py)
#   catboost    — model comparison (M2) + V1 legacy per-drug models
lightgbm==4.3.0
catboost==1.2.2
