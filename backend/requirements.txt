fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.2
pydantic-settings==2.1.0
python-multipart==0.0.6
httpx==0.27.2
numpy==1.24.3
pandas==2.1.4
# catboost (V1 CatBoost models) removed: V1 is disabled on this branch and the
# package is never imported at runtime. Re-add if V1 (/api/v1) is restored.
scikit-learn==1.3.2
joblib==1.3.2
# Per-prediction TreeSHAP explanations (M3/T3.1). Imported lazily and best-effort:
# if it fails to load (e.g. RAM-constrained host), recommendations still work and
# the explanation field is simply null. Remove this line to drop SHAP from deploy.
shap==0.45.1
